#include <iostream>

#include "doubleLL.h"

typedef struct player{
    //player *next, *prev;
    int arrow, hP, maxHP;
    int roll[5];
}Player;


using namespace std;

int main() {
    int numPlayers;
    cout << "Number of players: ";
    cin >> numPlayers;

    for(int)
    return 0;
}
